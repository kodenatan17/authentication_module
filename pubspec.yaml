name: authentication_module
description: "Authentication feature module — login, OTP, session management."
version: 0.0.1
publish_to: 'none'
homepage:

environment:
  sdk: ^3.10.9
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.8
  json_annotation: ^4.9.0
  get_it: ^7.7.0
  injectable: ^2.5.0
  dio: ^5.5.0+1
  flutter_bloc: ^8.1.6
  go_router: ^14.2.7
  hive: ^2.2.3
  core_module:
    path: ../core_module

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.11
  injectable_generator: ^2.6.2
  json_serializable: ^6.8.0

flutter:
